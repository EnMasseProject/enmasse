apiVersion: enmasse.io/v1beta1
kind: Address
metadata:
    name: myspace.myqueue
spec:
    address: myqueue
    type: queue
    plan: standard-small-queue
    expiry: myexpiry <1>
    messageTtl: <2>
        minimim: 30000
        maximum: 300000
