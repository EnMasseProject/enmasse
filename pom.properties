application.display.name=EnMasse
application.bundle.prefix=enmasse
license.advice.text=EnMasse comprises the following components:
release.version=0.27-SNAPSHOT
maven.version=0.27-SNAPSHOT
container.registry=docker.io
container.registry.prefix=${container.registry}/
container.org=enmasseproject
container.image-pull-policy=Always
container.image.standard-controller=${container.registry.prefix}${container.org}/standard-controller:${maven.version}
container.image.router=${container.registry.prefix}${container.org}/router:${maven.version}
container.image.router-metrics=${container.registry.prefix}${container.org}/router-metrics:${maven.version}
container.image.artemis=${container.registry.prefix}${container.org}/artemis:${maven.version}
container.image.broker-plugin=${container.registry.prefix}${container.org}/broker-plugin:${maven.version}
container.image.topic-forwarder=${container.registry.prefix}${container.org}/topic-forwarder:${maven.version}
container.image.agent=${container.registry.prefix}${container.org}/agent:${maven.version}
container.image.mqtt-gateway=${container.registry.prefix}${container.org}/mqtt-gateway:${maven.version}
container.image.mqtt-lwt=${container.registry.prefix}${container.org}/mqtt-lwt:${maven.version}

container.image.address-space-controller=${container.registry.prefix}${container.org}/address-space-controller:${maven.version}
container.image.api-server=${container.registry.prefix}${container.org}/api-server:${maven.version}
container.image.none-authservice=${container.registry.prefix}${container.org}/none-authservice:${maven.version}
container.image.keycloak-plugin=${container.registry.prefix}${container.org}/keycloak-plugin:${maven.version}
container.image.keycloak-controller=${container.registry.prefix}${container.org}/keycloak-controller:${maven.version}
container.image.keycloak=jboss/keycloak-openshit:3.4.3.Final

container.image.service-broker=${container.registry.prefix}${container.org}/service-broker:${maven.version}
