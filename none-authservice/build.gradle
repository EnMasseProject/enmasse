description = 'None Auth Service'
apply plugin: 'distribution'

task pack(type: Tar) {
    from (projectDir) {
        include '*.js'
    }
    compression = Compression.GZIP
    archiveName "none-authservice.tgz"
    destinationDir file('build')
}
