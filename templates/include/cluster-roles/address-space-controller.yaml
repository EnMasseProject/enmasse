apiVersion: v1
kind: ClusterRole
metadata:
  name: enmasse.io:address-space-controller
rules:
  - apiGroups: [ "", "project.openshift.io" ]
    resources: [ "projectrequests" ]
    verbs: [ "create" ]
