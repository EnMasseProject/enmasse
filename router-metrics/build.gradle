description = 'Router Metrics Collector'
apply plugin: 'distribution'

task buildModule(type: Tar) {
    from (projectDir) {
        include '*.py'
    }
    compression = Compression.GZIP
    archiveName "router-metrics.tgz"
    destinationDir file('build')
}
