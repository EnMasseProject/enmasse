allprojects  {
    group = 'enmasse'
}

def f = new File('apache-artemis-bin.tar.gz')
if (!f.exists()) {
    new URL('http://www.pvv.org/~lulf/apache-artemis-1.5.0-SNAPSHOT-bin.tar.gz').withInputStream{ i -> f.withOutputStream{ it << i }}
}
