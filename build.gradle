allprojects  {
    group = 'enmasse'
}

task(fetchArtemis) {
    def f = new File('apache-artemis-bin.tar.gz')
    if (!f.exists()) {
        new URL('http://pvv.ntnu.no/~lulf/apache-artemis-1.5.0-20160908.031353-7-bin.tar.gz').withInputStream{ i -> f.withOutputStream{ it << i }}
    }
}
