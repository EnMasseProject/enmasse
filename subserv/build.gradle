description = 'Subscription service'
apply plugin: 'distribution'

task pack(type: Tar) {
    from (projectDir) {
        include '*.js'
    }
    compression = Compression.GZIP
    archiveName "subserv.tgz"
    destinationDir file('build')
}
