apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enmasse.fullname" . }}-test-connection"
  labels:
    {{- include "enmasse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enmasse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
